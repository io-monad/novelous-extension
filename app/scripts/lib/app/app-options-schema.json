{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": [
    "updatePeriodMinutes",
    "notificationSettings"
  ],
  "properties": {
    "updatePeriodMinutes": {
      "type": "integer",
      "minimum": 30,
      "default": 30,
      "enum": [30, 60, 120, 180, 720, 1440],
      "enumNames": [
        "every 30 minutes",
        "every 1 hour",
        "every 2 hours",
        "every 3 hours",
        "every 12 hours",
        "every 24 hours"
      ]
    },
    "notificationSettings": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "alertEnabled": {
          "type": "boolean",
          "default": true
        },
        "soundEnabled": {
          "type": "boolean",
          "default": true
        },
        "autoCloseSeconds": {
          "type": "number",
          "minimum": 0,
          "default": 5
        }
      }
    }
  },
  "uiSchema": {
    "ui:order": [
      "updatePeriodMinutes",
      "notificationSettings"
    ],
    "ui:rootFieldId": "options",
    "notificationSettings": {
      "ui:order": [
        "soundEnabled",
        "alertEnabled",
        "autoCloseSeconds"
      ],
      "autoCloseSeconds": {
        "ui:help": "autoCloseSecondsHint"
      }
    }
  }
}
