import React from "react";
import MessageBox from "../common/message-box";

const SavingMessage = () => (
  <MessageBox message="saving" icon="circle-o-notch" spin />
);

export default SavingMessage;
